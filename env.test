# passwords/etc can be stored here, it's only for testing
POSTGRES_PASSWORD=postgres-test-cqXk1KkjDrnaUlgtVDoGzA
# postgres volume could be a path too, but we'll use a named volume now
POSTGRES_VOLUME=sovryn-nbte-bridge_postgres-test
ORD_POLLING_INTERVAL=200ms
